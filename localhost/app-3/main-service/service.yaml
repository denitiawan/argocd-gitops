apiVersion: v1
kind: Service
metadata:
  labels:
    app: main-service-app-3
  name: main-service-app-3
  namespace: app-3
spec:
  ports:
    - port: 80
      protocol: TCP
      targetPort: 80
  selector:
    app: main-service
  type: ClusterIP
